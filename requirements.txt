streamlit==1.45.0
plotly==5.24.1
pandas==2.2.3
openpyxl==3.1.5
numpy==1.26.4
python-dotenv==1.0.1

# ── CrewAI — version stable avec ses dépendances compatibles ──
crewai==0.28.0
crewai-tools==0.4.6

# ── LangChain stack — versions cohérentes entre elles ──
langchain==0.1.20
langchain-core==0.1.53
langchain-community==0.0.38
langchain-openai==0.1.8
langchain-groq==0.1.9

# ── LLM clients ──
openai==1.35.0
groq==0.9.0

# ── tiktoken — wheel pré-compilé disponible pour Python 3.11/3.12 ──
# NE PAS inclure tiktoken directement : il sera tiré par langchain-openai
# Si nécessaire, forcer la version avec wheel binaire :
tiktoken==0.7.0

# ── Autres dépendances ──
pydantic==2.7.4
tenacity==8.4.2
requests==2.32.3
